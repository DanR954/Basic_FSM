Create a template of a FSM to learn some of the basics as well as getting used to C++. 

Things learnt
- Currently in the program I am using new/delete which is manually allocating to the heap and should be replaced by smart pointers in the future to utilise the destructors
- use virtual states to hold the base methods that the child states can use.
